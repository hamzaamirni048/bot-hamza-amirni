version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./session:/app/session
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8000
    ports:
      - "8000:8000"
    restart: always
